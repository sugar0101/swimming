rules_version = '2';

// Lectura autenticada; escritura solo desde functions (Admin SDK).
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
